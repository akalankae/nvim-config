read last colorscheme
get appropriate background
check colorscheme database to see if last colorscheme has required background
if yes, apply colorscheme and background
if no, apply alternative colorscheme with background

find alternative colorscheme
if github_dark_* apply github_light_*
if github_dimmed apply github_light
if catppuccin-* apply catppuccin
if kanagawa-* apply kanagawa
if moonfly apply PaperColor
